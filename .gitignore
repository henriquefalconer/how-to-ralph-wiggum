node_modules/
dist/
.next/
.env
.DS_Store
.omc/
.omx/state/
.omx/metrics.json
# Track only QA/review-relevant logs; ignore noisy runtime hook logs.
.omx/logs/*.jsonl
!.omx/logs/omx-*.jsonl
!.omx/logs/session-history.jsonl
!.omx/logs/notify-fallback-*.jsonl

# Runtime files
*.log
# Loop runtime state lives in ralph-to-ralph/.state/ — ignored by that
# directory's own .gitignore.

# TypeScript incremental build cache
tsconfig.tsbuildinfo
